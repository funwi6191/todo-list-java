# ✅ To-Do List (Java, ArrayList + LocalDate)

A simple console-based To-Do List application built with **Java**. It uses `ArrayList` to store tasks and `LocalDate` to manage due dates, allowing users to add, view, and remove tasks efficiently.

---

## 🚀 Features
- Add new tasks with a description and due date
- View all tasks with deadlines
- Mark tasks as completed
- Remove tasks from the list
- Uses `ArrayList` for dynamic storage
- Uses `LocalDate` for date handling

---

## 🛠️ Installation

Clone the repository:
```bash
git clone https://github.com/funwi6191/todo-list-java.git
cd todo-list-java
